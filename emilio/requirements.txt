# Dipendenze di Emilio. Il nucleo (persona + supervisore + pipeline) gira con
# la sola libreria standard di Python: le righe qui sotto servono solo per i
# componenti "reali" (LLM, voce, hardware) e sono tutte opzionali.

# --- Base LLM (cervello reale) ---
# anthropic>=0.40

# --- Voce TTS ---
# requests>=2.31      # per ElevenLabs (TTS cloud realistico)
# pyttsx3>=2.90       # per TTS offline (alternativa)

# --- Hardware / movimento ---
# pyserial>=3.5       # per il controllo motori via seriale (Arduino/driver)
